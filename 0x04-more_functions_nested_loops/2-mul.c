#include "holberton.h"

/**
 * mul - a function to multiple to ints
 *
 * @a: an integer
 * @b: an integer
 *
 * Return: the value of sum;
 */

int mul(int a, int b)
{
	int sum = (a * b);

	return (sum);
}

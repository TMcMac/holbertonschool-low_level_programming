#include "calc.h"

/**
 * @op_add - a function to add two ints
 * @a: a first int
 * @b: a second int
 * Return: the result of the addition.
 */

int op_add(int a, int b)
{
	int result = a + b;
	
	return (result);
}


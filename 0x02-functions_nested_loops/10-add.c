#include "holberton.h"
#include <unistd.h>


/**
 * add - a function to add two ints
 * and return the sum.
 *
 * @a: the first of two ints from main
 * @b: the second of two ints from main
 *
 * Return: the value of the two ints
 * stored in the var sum.
 */

int add(int a, int b)
{
	int sum = (a + b);

	return (sum);
}

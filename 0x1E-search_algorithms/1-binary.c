#include "search_algos.h"
/**
 *
 *
 */


int binary_search(int *array, size_t size, int value)
{
  unsigned int mid, min, max;

  if (array == NULL || size <= 0)
    {
      return (-1);
    }

  i = (size / 2);
  if (array[i] == value)
    {
      
    }
  
}

